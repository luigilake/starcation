require 'rails_helper'

feature "edit celestials" do

  # let(:user) { User.create(username: 'testname', first_name: 'Jon', last_name: 'Smith', email: '123@gmail.com', password: '1234567') }
  # let(:planet) { Celestial.create(name: 'Mars', distance: '50', celestial_type: 'planet', size: '100', user: user) }
  # login_as(:user)
  #
  # xscenario 'form shows current information, specify a valid change' do
  #
  #
  # visit edit_celestial_path(planet)
  # expect(page).to have_content("Mars")
  # expect(page).to have_content("50")
  # expect(page).to have_content("Planet")
  # expect(page).to have_content("100")

# end

end
